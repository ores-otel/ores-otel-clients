# Conformance

`contracts/` supplies structural authority inputs; `conformance/` supplies shared implementation-neutral behavioral expectations across the polyglot SDKs. Bootstrap metadata is not behavioral coverage. Promotion evidence must bind the exact contract, corpus, and spec digests and fail closed when required evidence is missing, stale, failed, or mismatched.
